services:
  - type: web
    name: bot-plataform-backend
    runtime: node
    rootDir: .
    buildCommand: bash build.sh
    startCommand: NODE_ENV=production node apps/backend/dist/main
    healthCheckPath: /api/health
    envVars:
      - key: USE_SQLITE
        value: false
